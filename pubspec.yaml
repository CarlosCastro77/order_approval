name: order_approval
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  equatable: ^2.0.3
  firebase_auth: ^3.3.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  faker: ^2.0.0
  flutter_lints: ^1.0.0
  test: ^1.19.5
  mocktail: ^0.3.0

flutter:
  uses-material-design: true